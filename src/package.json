{
  "scripts": {
    "build": "npm run format && tsc --noEmit && npm run lint && npm run bundle && cp -rf manifest.json styles.css threadloaf-128.jpg popup.html test-data ../dist/",
    "bundle": "esbuild content_script.ts --bundle --outfile=../dist/content_script.js --format=iife --platform=browser --sourcemap && esbuild popup.ts --bundle --outfile=../dist/popup.js --format=iife --platform=browser --sourcemap",
    "format": "prettier --write \"**/*.ts\"",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@typescript-eslint/eslint-plugin": "^8.19.0",
    "@typescript-eslint/parser": "^8.19.0",
    "esbuild": "^0.20.1",
    "eslint": "^9.17.0",
    "globals": "^14.0.0",
    "prettier": "^3.4.2",
    "typescript": "^5.3.3"
  },
  "type": "module"
}
